import { DirecitveDirective } from './direcitve.directive';

describe('DirecitveDirective', () => {
  it('should create an instance', () => {
    const directive = new DirecitveDirective();
    expect(directive).toBeTruthy();
  });
});
